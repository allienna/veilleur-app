# Deterministic installs: fail if the lockfile would change in CI.
# (CI passes --frozen-lockfile explicitly; this keeps local installs honest.)
auto-install-peers=true
strict-peer-dependencies=false
