# pnpm install behaviour. Lockfile enforcement is NOT set here on purpose —
# CI passes `--frozen-lockfile` explicitly; local installs stay convenient.
auto-install-peers=true
strict-peer-dependencies=false
